
using AsemblyTest;
using AssemblyLib;

namespace AssemblyLib

{


    public class Program
    {
       

        private static void Main(string[] args)
        {
           
            // Console.WriteLine("Hello, World!");
            // Claculator calsi = new Claculator();
            // Console.WriteLine(calsi.add(10,20));
            // Console.WriteLine(calsi.sub(100, 20));
            //  Console.WriteLine(calsi.mul(10, 20));
            // Console.WriteLine(calsi.div(10, 2));
            Console.WriteLine(ElectricityBill.Caluclatebill(150));
           ElectricityBill.demo();
           // bill.demo();
           //  ReflectionDemo.demo();


        }
    }
}
